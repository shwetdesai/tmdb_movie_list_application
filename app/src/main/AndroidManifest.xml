<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shwet.mymovielistapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.MyMovieListApplication"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".screen.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyMovieListApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screen.MovieDetailsActivity"
            android:label="@string/title_activity_movie_details"
            android:theme="@style/Theme.MyMovieListApplication.NoActionBar" />
        <activity
            android:name=".screen.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyMovieListApplication.NoActionBar">

        </activity>
    </application>

</manifest>